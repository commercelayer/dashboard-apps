{
  "private": true,
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "dev": "pnpm --filter index dev",
    "build:elements": "pnpm --filter app-elements build",
    "build:docs": "pnpm --filter docs build",
    "build:apps-old": "pnpm --filter \"app-*\" build",
    "build:apps": "pnpm --filter build-apps start",
    "lint": "pnpm -r lint",
    "lint:fix": "pnpm -r lint:fix",
    "test": "pnpm -r test --stream",
    "ts:check": "pnpm -r ts:check",
    "test:watch": "pnpm -r test:watch",
    "dep:major": "pnpm dlx npm-check-updates --packageFile '**/package.json' -u",
    "dep:minor": "pnpm dep:major -t minor",
    "prepare": "husky install"
  },
  "keywords": [],
  "repository": {
    "type": "git",
    "url": "https://github.com/commercelayer/dashboard-apps"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "husky": "^9.1.1",
    "lerna": "^8.1.7",
    "lint-staged": "^15.2.7"
  },
  "resolutions": {
    "tar": "^6.2.1"
  }
}
